# 觅投AI (mitouai) — Python 依赖

# ── Web 框架 ──
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.5.0
python-dotenv>=1.0.0

# ── 认证 ──
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
bcrypt==4.0.1

# ── LLM 客户端 ──
openai>=1.12.0

# ── 市场数据 ──
akshare>=1.14.0
pandas>=2.1.0
stockstats>=0.6.2

# ── 量化因子 (Qlib Alpha158) ──
pyqlib>=0.9.6
numpy>=1.24.0
scipy>=1.10.0
pyarrow>=10.0.0
mlflow-skinny>=2.0.0
dill>=0.3.6
filelock>=3.9.0

# ── 异步 ──
httpx>=0.26.0
aiofiles>=23.2.0
sse-starlette>=1.8.0

# ── 部署 ──
gunicorn>=21.2.0

# ── 工具 ──
requests>=2.31.0
